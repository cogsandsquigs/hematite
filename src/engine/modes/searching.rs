// This is the main code for the minimax algorithm that drives the searching mode.

use crate::{engine::Engine, game::moves::Move};

/// Engine API for minimax.
impl Engine {
    /// Get the best move for the current state.
    pub fn searching_move(&self) -> Option<Move> {
        todo!()
    }
}
